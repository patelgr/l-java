/**
 * l-java: learning Java with production-grade tooling.
 *
 * <p>All types in this package are non-null by default (JSpecify {@code @NullMarked}).
 * Annotate with {@code @Nullable} only where null is an intentional part of the contract.
 */
@NullMarked
package io.github.patelgr.ljava;

import org.jspecify.annotations.NullMarked;
